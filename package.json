{
  "name": "realestate-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:seed": "npm run prisma:seed --prefix backend",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && cp -n .env.example .env 2>/dev/null || true"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
